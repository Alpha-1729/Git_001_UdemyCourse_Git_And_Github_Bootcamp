#!/bin/bash
# Switching Branches With Unstaged Changes

:"
>>>> If you create a new files in the current branch.
        Then checking out to other branches is possible.
        The new file created will be available in that branch.
>>>> If you modify an existing file in the current branch.
        Then you need to commit the changes in that file, before switch into another branch that has the same file.
>>>>
>>>>
"