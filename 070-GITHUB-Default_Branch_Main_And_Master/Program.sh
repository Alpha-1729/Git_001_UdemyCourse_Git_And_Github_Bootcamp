#!/bin/bash
# Default Branch Main And Master

:"
>>>> If we create a repo in github without any files like Readme or .gitignore, the default branch will be master.
>>>> Otherwise the default branch created will be main.
>>>> The default branch can be changed from the settings of that particular repo.
>>>> Renaming branch master to main
        git branch -M main
"