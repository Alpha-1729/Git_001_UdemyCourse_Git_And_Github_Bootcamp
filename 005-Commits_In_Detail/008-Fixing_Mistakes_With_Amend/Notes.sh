#!/bin/bash
# Fixing Mistakes With Amend

:"
>>>> Git commit docs.
        Reference Link: https://git-scm.com/docs/git-commit
>>>> To add a file to the previous commit.
        git add forgotten-file
        git commit --amend (You can also update the previous commit message)
>>>> To update only the commit message.
        git commit --amend
>>>>
"
