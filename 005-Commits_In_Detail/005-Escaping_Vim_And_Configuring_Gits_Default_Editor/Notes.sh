#!/bin/bash
# Escaping Vim And Configuring Gits Default Editor

:"
>>>> Configuring the Gits default editor.
        Reference Link: https://git-scm.com/book/en/v2/Appendix-C%3A-Git-Commands-Setup-and-Config
>>>> Setting the VsCode as the default editor for git commit.
        `git config --global core.editor "code --wait"`
>>>> Steps for Committing Changes Using VIM:
        1. Run git commit.
        2. Press "i" to enter insert mode.
        3. Type your commit message.
        4. Press the "Esc" key to exit insert mode.
        5. Type :wq to write changes and quit VIM.
        Note: wq means write and quit.
>>>>
>>>>
"
