#!/bin/bash
# Introducing Git Push

:"
>>>> Git push Docs:
        Reference Link: https://git-scm.com/docs/git-push
>>>> git push is not a github specific command.
        We can use git push command to push our repo into any hosting service.
>>>> Renaming branch master to main before pushing.
        git checkout master
        git branch -M main
        git push -u origin main
>>>> We don't have to be in the same branch to push that branch to github.
"