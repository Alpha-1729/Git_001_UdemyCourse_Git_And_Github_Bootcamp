#!/bin/bash
# Resolving Mr With Conflicts

:"
>>>> Your can directly resolve conflicts from the github UI.
>>>> Or you can resolve conflicts from the command line and push the master branch to origin.
        Please check the screenshot attached.
        --no-ff  -> means to avoid the fast-forwarded merge to create a commit message while merging feature-branch into main.
>>>>
>>>>
"